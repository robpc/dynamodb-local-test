# dynamodb-local-test

Example project that locks up node over time.

## Getting Started

    $ npm install
    $ npm run setup
    $ npm start

In a separate terminal

    $ ./test.sh

